const router=require('express').Router(); const multer=require('multer'); const path=require('path'); const fs=require('fs'); const c=require('../controllers/resumeController'); const {requireAuth}=require('../middleware/authMiddleware');
const dir=path.join(__dirname,'..','uploads'); fs.mkdirSync(dir,{recursive:true}); const storage=multer.diskStorage({destination:dir,filename:(_r,f,cb)=>cb(null,`${Date.now()}-${f.originalname.replace(/[^a-zA-Z0-9._-]/g,'_')}`)}); const upload=multer({storage,limits:{fileSize:10*1024*1024},fileFilter:(_r,f,cb)=>{const ok=['.pdf','.doc','.docx','.txt'].includes(path.extname(f.originalname).toLowerCase()); cb(ok?null:new Error('Only PDF, DOC, DOCX and TXT resumes are supported.'),ok);}});
router.post('/upload',requireAuth,upload.single('resume'),c.uploadResume); router.get('/analysis',requireAuth,c.analysis); module.exports=router;
